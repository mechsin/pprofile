#
# ~/.bash_profile
#
# Just use the .bash_profile to source the .profile 
# and the .bashrc if you want to add something here 
# add it there instead

# Load .profile containing login, non-bash related initializations 
source ~/.profile

# Load .bashrc, contiaing non-login related bash initializations
source ~/.bashrc
